# Works on Python 3.9 – 3.13.
# Compiled packages use floors (so pip grabs a wheel built for your Python);
# streamlit is pinned to keep the app's API stable.
streamlit==1.50.0
pandas>=2.2.3
numpy>=2.1,<3
plotly>=6.0
scikit-learn>=1.5
sentence-transformers>=3.0
groq>=0.11
